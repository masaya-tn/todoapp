.container               
  = form_with(model: task, url: board_tasks_path(@task.board), local: true) do |f|
    %div
      = f.label :eyecatch, 'アイキャッチ'
    %div
      = f.file_field :eyecatch
    %div
      = f.label :title, 'タイトル'
    %div
      = f.text_field :title, class: 'title_form'
    %div
      = f.label :content, '内容'
    %div
      = f.text_area :content, class: 'content_form'
    %div
      = f.label :limit, '期限'
    %div
      = f.date_field :limit
    %div
      = f.hidden_field :user_id, :value => user_id
    = f.submit 'submit', class: 'btn-primary'

-# url: board_tasks_path(@task.board)